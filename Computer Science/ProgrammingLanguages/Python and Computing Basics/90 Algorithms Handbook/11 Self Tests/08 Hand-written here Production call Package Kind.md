# Hand-written here Production call Package Kind

Hand-written here Production call Package Kind
fibonacci_memoised @functools.cache functools standard library
knapsack_01 scipy.optimize.milp(...) scipy third-party
longest_common_subsequence LCSseq.similarity(a, b) rapidfuzz third-party
edit_distance Levenshtein.distance(a, b) rapidfuzz third-party
longest_increasing_subsequenc
e_length
bisect.bisect_left bisect standard library
maximum_subarray_sum itertools.accumulate(numbers, func) itertools standard library
fractional_knapsack scipy.optimize.linprog(...) scipy third-party
huffman_codes zlib.compress(data) zlib standard library
huffman_codes heapq.heappush / Counter heapq / collections standard library
generate_subsets itertools.combinations(items, size) itertools standard library
generate_permutations itertools.permutations(items) itertools standard library
gcd_euclid math.gcd(a, b) math standard library
lcm math.lcm(a, b) math standard library
fast_power pow(base, exponent, modulus) built-in built-in
sieve_of_eratosthenes sympy.primerange(lo, hi) sympy third-party
is_prime_trial_division sympy.isprime(n) sympy third-party
prime_factorisation sympy.factorint(n) sympy third-party
kmp_search text.find(pattern) · re.finditer(...) built-in / re built-in
rabin_karp_search re.finditer('|'.join(patterns), text) re standard library
is_palindrome_two_pointers letters == letters[::-1] built-in built-in
maximum_sum_fixed_window accumulate(numbers, initial=0) itertools standard library
build_prefix_sums accumulate(numbers, initial=0) itertools standard library
reverse_linked_list deque.reverse() · reversed(items) collections standard library
CS Algorithms Toolkit From scratch to production
8
