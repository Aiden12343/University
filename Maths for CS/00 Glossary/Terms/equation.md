# equation
A statement that two expressions are equal, written with an equals sign.
## First introduced in
[[2.1 Variables Expressions and Equations]]
## Builds on
[[expression]]
## Used in CS
Constraints such as `used + buffer = limit` or balance checks in algorithms are written as equations that must hold for a valid state.
