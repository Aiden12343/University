# 11.9 Linear Algebra for Representation Learning - Practice Questions

Theory note: [[11.9 Linear Algebra for Representation Learning]]

Do these on paper first. Check [[11.9 Linear Algebra for Representation Learning - Answers]] only after making a serious attempt.

## Vocabulary
1. Define embedding space.
2. Define projection.
3. Define low-rank approximation.
4. Write one sentence explaining the difference between two of the new words.

## Core Skill
5. Compute the SVD of A = [[1,1],[0,0],[1,1]]. Find the non-zero singular value(s) and the corresponding left and right singular vectors.

6. Using the SVD from Q5, compute the optimal rank-1 approximation A₁. Compute the Frobenius norm error ||A − A₁||_F. Verify the Eckart-Young theorem by confirming that this error equals the next (omitted) singular value.

7. Now consider A' = [[2,0],[0,2],[1,1]]. Compute its SVD and optimal rank-1 approximation A'₁. Compare how well a rank-1 approximation captures the structure in A' versus in A.

8. Explain each step of the SVD computation from Q5. Specifically, explain why the eigenvectors of AᵀA give the right singular vectors and why Av_i/σ_i gives the left singular vectors.

## Error Checking
9. A student performs PCA on the data points [1,2], [2,3], [3,4] by computing the SVD of the raw (uncentred) data matrix X and taking the first right singular vector as the first principal component. Identify the exact error and explain why centring the data (subtracting the mean) is necessary for PCA.

10. For a user-item matrix R with observed entries R₁₁=5, R₁₂=3, R₂₁=4, R₂₂=2 and factors P = [[0.5,0.5],[0.9,0.1]], Q = [[0.6,0.4],[0.1,0.9],[0.7,0.3],[0.8,0.2]], verify the predicted rating for user 2, item 4. Compute r̂₂₄ = p₂ · q₄ and confirm the value.

## CS Transfer
11. In recommendation systems, matrix factorisation R ≈ P·Qᵀ learns latent user and item features. Explain the "cold start" problem: what happens when a new user with no ratings joins the system, and how might this be addressed?

12. Word embeddings exhibit the property vec(king) − vec(man) + vec(woman) ≈ vec(queen). State the mathematical operation this represents and explain why this linear structure in embedding space is useful for downstream NLP tasks such as analogy completion.

Pass target: 85%. Definitions must be perfect.
