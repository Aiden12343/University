# 3.4 Quadratic Functions - Practice Questions

Theory note: [[3.4 Quadratic Functions]]

Answers: [[3.4 Quadratic Functions - Answers]]

> [!tip] Paper-first rule
> Sketch a rough parabola for Sections B-E before checking answers. Your sketch is a fast error detector.

## How to Use This Practice
Do the sections in order.

For every algebra question, write the reason for the first step:

```text
highest power
sign of a
zero product rule
factorising
substitution check
```

If you cannot name the reason, reread the matching concept in [[3.4 Quadratic Functions]] before continuing.

## Section A - Vocabulary
1. Define [[parabola]].
2. Define [[turning point]].
3. Define [[roots]].
4. Define [[axis of symmetry]].
5. Define [[quadratic coefficient]].
6. In `y = ax^2 + bx + c`, which letter tells you whether the parabola opens up or down?

## Section B - Core Skills
7. State whether `y = 2x^2 - 3x + 1` is quadratic.
8. State whether `y = 7x - 4` is quadratic.
9. For `y = -x^2 + 5x + 6`, identify a, b, and c.
10. Does `y = -4x^2 + 2` open upward or downward?

For Questions 11-18, show at least one algebra line before the answer.

11. Find the roots of `y = (x - 3)(x + 5)`.
12. Find the roots of `y = (x + 1)(x + 8)`.
13. Factorise `x^2 + 7x + 10`.
14. Factorise `x^2 - 9x + 20`.
15. Solve `x^2 + 6x + 8 = 0`.
16. Solve `x^2 - 11x + 24 = 0`.
17. Solve `x^2 - 5x = 0`.
18. Find the y-intercept of `y = 3x^2 - 2x + 9`.

## Section C - Mistake-Spotting
For each question, write:

```text
Mistake:
Fix:
Reason:
```

19. A learner says `y = x^3 + 2x^2` is quadratic because it has an x squared term. Explain the mistake.
20. A learner solves `(x - 4)(x + 2) = 0` and gives only x = 4. What did they miss?
21. A learner says the axis of symmetry for roots 2 and 10 is `(6, 0)`. Correct the notation.
22. A learner expands `(x + 3)(x + 4)` as `x^2 + 7`. Find and fix the error.
23. A learner divides `x^2 + 2x = 0` by x and gets x = -2. What root is lost?

## Section D - Mixed Practice
For Questions 24-29, start with a rough sketch or a table of facts.

Useful table:

```text
a:
roots:
axis:
turning point:
y-intercept:
```

24. `y = (x + 2)(x - 6)`. Find the roots, axis of symmetry, and turning point.
25. `y = x^2 - 4x - 12`. Factorise and solve.
26. `y = -x^2 + 8x`. Find the roots and the axis of symmetry.
27. Is x = -3 a root of `y = x^2 + x - 6`? Show substitution.
28. Write a quadratic with roots x = 1 and x = -5.
29. Explain why roots are also called x-intercepts.

## Section E - CS Transfer
For each CS question, name the mathematical object and the programming object.

Example:

```text
Maths object: n^2
Programming object: nested loop comparison count
```

30. A nested loop runs n outer iterations and n inner iterations. Write the operation count as a function of n.
31. If `C(n) = n^2`, calculate C(20).
32. A simple height model is `h(t) = -t^2 + 6t`. When is the height 0?
33. A squared-error model is `E(x) = (x - 4)^2`. Where is its minimum?
34. Give one reason quadratic growth matters for algorithm performance.

## Stretch
35. For `y = (x - 2)(x - 8)`, find the turning point and explain whether it is a minimum or maximum.

## Self-Check Before Marking
- Did you define every vocabulary word without copying?
- Did you use both brackets when finding roots?
- Did you write axes as `x = number`?
- Did you check at least one root by substitution?
- Did your CS answer name a concrete program, loop, animation, or model?
