# 3.7 Logarithms - Practice Questions

Theory note: [[3.7 Logarithms]]

Answers: [[3.7 Logarithms - Answers]]

> [!tip] Paper-first rule
> Write the matching exponent statement beside every logarithm before trying to evaluate it.

## How to Use This Practice
Use this template:

```text
log statement:
power question:
exponential check:
answer:
```

## Section A - Vocabulary
1. Define [[logarithm]].
2. Define [[base]].
3. Define [[power]].
4. Define [[inverse operation]].
5. Define [[log laws]].
6. Explain why logarithms belong after [[3.6 Exponential Functions]].

## Section B - Core Skills
7. Evaluate `log_2(8)`.
8. Evaluate `log_2(32)`.
9. Evaluate `log_3(27)`.
10. Evaluate `log_5(125)`.
11. Convert `log_4(64) = 3` to exponential form.
12. Convert `10^3 = 1000` to logarithm form.
13. Solve `2^x = 128`.
14. Solve `5^x = 625`.
15. Simplify `log_2(8) + log_2(4)`.
16. Simplify `log_3(81) - log_3(9)`.
17. Simplify `log_2(16^3)`.
18. Write the exact solution to `7^x = 50`.

## Section C - Mistake-Spotting
For each question, write the mistake, correction, and reason.

19. A learner says `log_2(8) = 16`.
20. A learner rewrites `log_3(81) = 4` as `81^4 = 3`.
21. A learner says `log_2(8 + 4) = log_2(8) + log_2(4)`.
22. A learner says `log_10(1000) = 100`.
23. A learner says logarithmic algorithms are fast-growing because logs look complicated.

## Section D - Mixed Practice
24. Evaluate `log_10(100000)`.
25. Evaluate `log_4(16)`.
26. Solve `2^x = 1024`.
27. Simplify `log_5(25 * 125)`.
28. Simplify `log_2(64 / 8)`.
29. How many halvings take 2048 items down to 1?

## Section E - CS Transfer
30. A sorted list has 512 items. About how many binary-search halving steps are needed?
31. A balanced binary tree has 128 leaves. What is its depth if full and perfectly balanced?
32. How many bits identify one item out of 64 equally likely choices?
33. In one sentence, explain why `O(log n)` grows slowly.
34. Name one CS topic besides search where logarithms appear.

## Stretch
35. Simplify `log_2(32^2) - log_2(8)`, then check by evaluating the powers.

## Self-Check Before Marking
- Did you translate each log into a power question?
- Did you keep the base in the correct position?
- Did you avoid distributing logs over addition?
- Did you give exact log form when a decimal was not needed?
- Did your CS answer mention halving, bits, entropy, trees, or scale?

## Marking Routine
Mark in a different colour.

For each wrong answer, write:

```text
The log question was ...
The correct exponent statement is ...
```

Retake only the section that contained the error before attempting [[3.7 Logarithms - Mastery Test]].
